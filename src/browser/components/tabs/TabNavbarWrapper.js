import styled from 'styled-components';

// language=SCSS
const TabNavbarWrapper = styled.ul`
  & {
    margin: 0;
    padding: 0;
    list-style-type: none;
    text-align: center;
    width: 100%;
  }
`;

export default TabNavbarWrapper;
